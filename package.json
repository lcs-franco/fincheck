{
  "devDependencies": {
    "turbo": "^1.10.16"
  },
  "scripts": {
    "test": "cd apps/api && yarn test",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "private": true,
  "workspaces": [
    "apps/*"
  ]
}
